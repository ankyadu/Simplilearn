echo "# Simplilearn" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/ankyadu/Simplilearn.git
git push -u origin main
…or push an existing repository from the command line
git remote add origin https://github.com/ankyadu/Simplilearn.git
git branch -M main
git push -u origin main


Install git in windows, after install -> go to thee folder right click.. and open git bash here and use belo commands

git init
git clone
git status
git add
git status
git add.
git commit -m "Test" -? move files from staging to repo area.
git push -> move changes to repos -> get password from settings->dev setting ->generate classic tokens. commit -am "Comiiting new and old files toggether"

git push ->it will ask for password and token


git pull
git fethc ->update and local files

git checkout


https://github.com/skandhwa/1stJulySimplelearn